# Configuration de développement - Notification Service
# Désactive RabbitMQ pour tester SMTP uniquement

# Server Configuration
server.port=8087

# Application Name
spring.application.name=NOTIFICATION-SERVICE

# MySQL DataSource Configuration (BASE UNIQUE)
spring.datasource.url=jdbc:mysql://localhost:3306/iusjcdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=iusjc
spring.datasource.password=iusjc2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Désactiver RabbitMQ pour les tests
spring.rabbitmq.listener.simple.auto-startup=false
spring.rabbitmq.listener.direct.auto-startup=false

# Mail Configuration SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jacky.kouang@saintjeaningenieur.org
spring.mail.password=uvkb ltoz uuhx tizv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Management Endpoints
management.endpoints.web.exposure.include=health,info