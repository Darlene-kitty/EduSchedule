package cm.iusjc.scheduling.config;

import org.springframework.context.annotation.Configuration;

// Redis disabled - using simple in-memory cache (spring.cache.type=simple)
@Configuration
public class RedisConfig {
}
